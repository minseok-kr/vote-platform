services:
  vote-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - vote-data:/app/data
    restart: unless-stopped

volumes:
  vote-data:
